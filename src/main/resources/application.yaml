spring:
  application:
    name: service-discovery
  profiles:
    group:
      "local": "local, eureka, actuator"
      "docker": "docker, eureka, actuator"
      "prod": "prod, eureka, actuator"
    active: ${PROFILE_ACTIVE:local}

  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
