spring:
  application:
    name: service-discovery
  profiles:
    active: local
    include:
      - eureka
      - actuator

  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
